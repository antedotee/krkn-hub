name: Notify Docs Sync

# When a PR merges on this fork's docs-sync-test branch (or main, eventually),
# fire a `repository_dispatch` event to antedotee/krkn-website so its
# docs-sync orchestrator can decide whether to open a docs PR.
#
# This workflow is a thin trigger: NO logic lives here, NO LLM calls.
# All decision-making happens on the website side after dispatch.

permissions:
  contents: read
  pull-requests: read

on:
  pull_request:
    types: [closed]
    branches: [main, docs-sync-test]

  # Allow manual dispatch for testing without merging a real PR
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR number to simulate'
        required: true
      head_sha:
        description: 'Head SHA'
        required: true
      base_sha:
        description: 'Base SHA'
        required: true

jobs:
  dispatch:
    # Only fire on actual merges (not PR-closed-without-merging) for the
    # pull_request event. workflow_dispatch always passes.
    if: >
      github.event_name == 'workflow_dispatch'
      || (github.event.pull_request.merged == true)
    runs-on: ubuntu-latest
    steps:
      - name: Send repository_dispatch to website
        env:
          # WEBSITE_DISPATCH_PAT — fine-grained PAT scoped to
          # antedotee/krkn-website with `Actions: Write`.
          DISPATCH_PAT: ${{ secrets.WEBSITE_DISPATCH_PAT }}
          UPSTREAM_REPO: ${{ github.repository }}
          PR_NUMBER: >-
            ${{ github.event_name == 'workflow_dispatch'
                && github.event.inputs.pr_number
                || github.event.pull_request.number }}
          HEAD_SHA: >-
            ${{ github.event_name == 'workflow_dispatch'
                && github.event.inputs.head_sha
                || github.event.pull_request.merge_commit_sha }}
          BASE_SHA: >-
            ${{ github.event_name == 'workflow_dispatch'
                && github.event.inputs.base_sha
                || github.event.pull_request.base.sha }}
        run: |
          set -e
          echo "Dispatching docs-sync for ${UPSTREAM_REPO}#${PR_NUMBER}"
          # Build payload as compact JSON. Fail loud if any field empty.
          for var in UPSTREAM_REPO PR_NUMBER HEAD_SHA BASE_SHA; do
            if [ -z "${!var}" ]; then
              echo "::error::missing required field: ${var}"
              exit 1
            fi
          done

          curl -sS -X POST \
            -H "Authorization: token ${DISPATCH_PAT}" \
            -H "Accept: application/vnd.github+json" \
            -H "Content-Type: application/json" \
            "https://api.github.com/repos/antedotee/krkn-website/dispatches" \
            -d "$(cat <<EOF
          {
            "event_type": "docs-sync",
            "client_payload": {
              "upstream_repo": "${UPSTREAM_REPO}",
              "pr_number":     ${PR_NUMBER},
              "head_sha":      "${HEAD_SHA}",
              "base_sha":      "${BASE_SHA}"
            }
          }
          EOF
          )"

          echo '::notice::repository_dispatch sent successfully'
