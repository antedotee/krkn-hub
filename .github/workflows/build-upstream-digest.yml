name: Build Upstream Digest

# Regenerates .docs-sync-digest/ for krkn-hub on every change to scenario
# directories. The output is consumed by docs-sync's relevance gate in the
# website fork — when an upstream PR merges, the website pipeline reads
# .docs-sync-digest/llms-full.txt at the head and base refs to detect what
# user-facing surface area changed.

permissions:
  contents: write

on:
  push:
    branches: [main, docs-sync-test]
    paths:
      # Any change to scenario env.sh or krknctl-input.json triggers rebuild.
      - '*/env.sh'
      - '*/krknctl-input.json'
      - '.docs-sync/digest/**'
      - '.github/workflows/build-upstream-digest.yml'

  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual rebuild'
        required: false
        default: 'Manual trigger'

# One run at a time per branch — earlier runs cancelled if a newer push lands.
concurrency:
  group: build-upstream-digest-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-digest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 1

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build upstream digest
        run: python .docs-sync/digest/build_upstream_digest.py --repo-name krkn-hub

      - name: Commit digest back if changed
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add .docs-sync-digest/
          if git diff --staged --quiet; then
            echo '::notice::Digest unchanged — no commit needed.'
            exit 0
          fi
          git commit -m "chore(docs-sync): regenerate upstream digest

          Auto-regenerated by .github/workflows/build-upstream-digest.yml after
          changes to scenario configurations. Source commit: ${GITHUB_SHA::7}."
          git push origin HEAD:${GITHUB_REF#refs/heads/}
          echo '::notice::Digest committed.'
